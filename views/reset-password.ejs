<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="author" content="Abradu Frimpong Kwame" />
    <meta name="description" content="An Examination Website" />
    <meta name="keywords" content="Examination" />

    <link rel="icon" href="/images/favicon.ico" type="image/x-icon" />

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" crossorigin="anonymous" />

    <!-- AOS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" crossorigin="anonymous" />
    
    <title>Login</title>

    <style>
      .password-toggle {
        position: absolute;
        right: 1rem;
        top: 75%;
        transform: translateY(-50%);
        cursor: pointer;
        color: #6c757d;
      }

      .form-group {
        position: relative;
        margin-bottom: 1.5rem;
      }

      .headerText {
        text-align: center;
        margin-bottom: 1.5rem;
      }
    </style>
  </head>
  <body>
    <main role="main">
      <section class="login py-5" id="login">
        <div class="container">
          <div class="row justify-content-center">
            <div class="col-md-6">
              <header class="header">
                <h1 class="headerText" role="heading" id="loginHeading">Login</h1>
              </header>
              <form method="POST" action="/reset-password/<%= token %>">
                  <input type="password" name="password" placeholder="New password" required />
                  <button type="submit">Reset Password</button>
                </form>
                
                <% if (error) { %><div><%= error %></div><% } %>

              
            </div>
          </div>
        </div>
      </section>
    </main>

    <!-- Toggle Password Visibility Script -->
    <script>
      
    </script>
  </body>
</html>
