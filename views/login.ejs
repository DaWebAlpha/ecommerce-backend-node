<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="author" content="Abradu Frimpong Kwame" />
    <meta name="description" content="An Examination Website" />
    <meta name="keywords" content="Examination" />

    <link rel="icon" href="/images/favicon.ico" type="image/x-icon" />

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" crossorigin="anonymous" />

    <!-- AOS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" crossorigin="anonymous" />
    
    <link rel="stylesheet" href="css/style.css">
    <title>Login</title>

    <style>
      .password-toggle {
        position: absolute;
        right: 1rem;
        top: 75%;
        transform: translateY(-50%);
        cursor: pointer;
        color: #6c757d;
      }

      .form-group {
        position: relative;
        margin-bottom: 1.5rem;
      }

      .headerText {
        text-align: center;
        margin-bottom: 1.5rem;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-dark navbar-expand-lg fixed-top" style='background-color: #b323df'>
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Exams Portal</a>
      </div>
    </nav>
    <main role="main">
      <section class="login py-5 mt-5" id="login">
        <div class="container">
          <div class="row justify-content-center">
            <div class="col-md-6">
              <div class="card shadow-sm">
                <div class="card-body">
                  <header class="header">
                    <h1 class="headerText" id="loginHeading">Login</h1>
                  </header>

                  <form
                    action="/login"
                    method="POST"
                    role="form"
                    novalidate
                    autocomplete="off"
                    aria-labelledby="loginHeading"
                  >
                    <div class="form-group">
                      <label for="username" class="form-label">Username</label>
                      <input
                        type="text"
                        name="username"
                        class="form-control"
                        id="username"
                        aria-describedby="usernameHelp"
                        autocomplete="username"
                        required
                      />
                      <div class="validateNameMessage form-text"></div>
                    </div>

                    <div class="form-group">
                      <label for="password" class="form-label">Password</label>
                      <input
                        type="password"
                        name="password"
                        class="form-control"
                        id="password"
                        placeholder="Enter password"
                        aria-describedby="passwordHelp"
                        autocomplete="current-password"
                        required
                      />
                      <div id="passwordHelp" class="form-text validatePassword"></div>
                      <span 
                        class="password-toggle" 
                        id="togglePassword" 
                        role="button" 
                        aria-label="Toggle password visibility"
                        title="Toggle password visibility"
                      >
                        <i class="fas fa-eye"></i>
                      </span>
                    </div>

                    <% if (error) { %>
                      <div class="alert alert-danger mt-2" id="error-message" role="alert">
                        <%= error %>
                      </div>
                    <% } %>

                    <div class="d-grid">
                      <button type="submit" class="btn btn-success" aria-label="Submit login form">Login</button>
                    </div>

                    <div class="form-group d-flex justify-content-between mt-3">
                      <span>
                        <a href="/register" aria-label="Sign up link">Sign Up</a>
                      </span>
                      <span>
                        <a href="/forgot-password" aria-label="Forgot password link">Lost Password?</a>
                      </span>
                    </div>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>
    <%- include('partials/footer') %>

    
    
  </body>
</html>
